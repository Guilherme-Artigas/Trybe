const product = {
  model: 'Smart TV Crystal UHD',
  price: '1899.05',
  seller: 'Casas de Minas',
};

const {model, seller} = product;
// const {seller} = product;
console.log(`Modelo: ${model}, Loja: ${seller}`);
